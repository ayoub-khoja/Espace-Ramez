"""Modèles du portail (MVT). Étendre ici les entités métier (réservations, terrains, etc.)."""

from django.db import models

# Exemple d'ossature — à enrichir plus tard :
# class Reservation(models.Model):
#     court = models.CharField(max_length=120)
#     start = models.DateTimeField()
#     ...
