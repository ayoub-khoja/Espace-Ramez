{
  "name": "espace-ramez",
  "private": true,
  "description": "Scripts npm pour lancer le backend Django (dossier backend).",
  "scripts": {
    "dev": "python backend/manage.py runserver",
    "dev:pipenv": "pipenv run python backend/manage.py runserver",
    "migrate": "python backend/manage.py migrate",
    "migrate:pipenv": "pipenv run python backend/manage.py migrate",
    "seed": "python backend/manage.py seed_admin",
    "seed:pipenv": "pipenv run python backend/manage.py seed_admin"
  }
}
